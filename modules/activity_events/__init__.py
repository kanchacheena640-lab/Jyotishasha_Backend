# modules/activity_events/__init__.py
#
# Package for the Phase 2 first-party activity_events ledger:
# event_schemas.py (the property allowlist/validator) and service.py (the
# shared write helper). Deliberately not wired into any producer yet --
# see modules/activity_events/service.py's own docstring.
